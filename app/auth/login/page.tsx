import { LoginForm } from "@/components";

const LoginPage = () => {
    return (
        <main className=" overflow-hidden">
            <LoginForm />
        </main>
    );
};

export default LoginPage;