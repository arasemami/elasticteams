import { LoginForm } from "@/app/components";

const LoginPage = () => {
    return (
        <main className=" overflow-hidden">
            <LoginForm />
        </main>
    );
};

export default LoginPage;