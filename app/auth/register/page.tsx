import { RegisterForm } from "@/app/components";

const RegisterPage = () => {
    return (
        <main className=" overflow-hidden">
            <RegisterForm />
        </main>
    );
};

export default RegisterPage;